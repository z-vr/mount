{
  "name": "koa-mount",
  "description": "Mounting middleware for koa",
  "repository": "https://github.com/z-vr/mount",
  "version": "3.1.0",
  "keywords": [
    "koa",
    "middleware",
    "mount",
    "mounting"
  ],
  "files": [
    "index.js"
  ],
  "devDependencies": {
    "eslint": "^4.17.0",
    "eslint-config-standard": "^11.0.0-beta.0",
    "eslint-plugin-promise": "^3.6.0",
    "eslint-plugin-standard": "^3.0.1",
    "istanbul": "^0.4.5",
    "koa": "^2.5.0",
    "koa-static": "^4.0.2",
    "mocha": "^5.0.0",
    "should": "^13.2.1",
    "supertest": "^3.0.0"
  },
  "license": "MIT",
  "dependencies": {
    "debug": "^3.1.0",
    "koa-compose": "^4.0.0"
  },
  "scripts": {
    "lint": "eslint --fix .",
    "test": "NODE_ENV=test mocha --reporter spec --exit",
    "test-cov": "NODE_ENV=test istanbul cover ./node_modules/.bin/_mocha",
    "test-travis": "NODE_ENV=test istanbul cover ./node_modules/.bin/_mocha --report lcovonly"
  },
  "engines": {
    "node": ">= 7.6.0"
  }
}
